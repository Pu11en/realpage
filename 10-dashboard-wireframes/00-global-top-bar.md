Status: shared on every frame — not a standalone page

# Global top bar (appears on all 5 frames)

- Left: **PropertyStack** wordmark
- Center: 4 tabs — Early Leads · Master Table · Software Share · Under the Hood
  (Property Detail has no tab of its own — reached by clicking a row)
- Right: **Area** dropdown ("Plano, Richardson"), **View as** switch
  (Neutral / RealPage / Yardi / Entrata), "Last updated: Sep 10, 2026"
- **View as** behavior (describe in every prompt that needs it): the chosen
  vendor's color is highlighted across the page; rows/segments already on
  that vendor look dimmed/muted; everything else reads as a "target" —
  in v1 wireframes we'll show the switch set to **Neutral** by default so
  no single vendor's color dominates the mockup.

Desktop only for all frames — phone layout comes later.
