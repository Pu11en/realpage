# What are we selling? — planning brainstorm (2026-09-12)

Status: brainstorm in progress. No code changes. Drew's answers get added below as we go.

## Plain facts: what RealPage is

- RealPage sells **software to companies that own or manage apartment buildings** (landlords with hundreds or thousands of units).
- The software runs the building's business: leasing, rent collection, pricing rents, resident apps, accounting.
- Their rivals sell the same kind of software: **Yardi, Entrata, AppFolio, MRI, Buildium**.
- So RealPage makes money by **winning buildings from those rivals** (and keeping its own). Every building runs on one of these systems.
- Public image is bad: the DOJ sued them over their rent-pricing algorithm; Reddit is openly hostile (posts with 15k+ upvotes).

## What PropertyStack already does (the starting point)

- Finds apartment buildings in an area and works out **which of these software systems each one uses**.
- Ranks buildings by early signs they may switch (Early Leads = a call list).
- A chat agent answers questions, does web lookups, and prepares for calls.
- Live on Railway with Google sign-in.

## The one-sentence pitch (draft, to test)

- "PropertyStack tells your sales team **which apartment buildings run a competitor's software, and which ones are likely to switch soon**, with a ready call sheet for each."
- Deliverable = a tool + a weekly ranked call list, not a report.

## Open questions (answered one at a time)

1. ⏳ Who is the buyer: RealPage sales, RealPage's AI team (the interview), rivals' sales, or property managers?
2. ⬜ What does "production-ready to sell" mean for that buyer (copy, onboarding, proof, price)?
3. ⬜ Language check: pull Reddit posts from salespeople and property managers to learn how they talk.
4. ⬜ Easy wins: what can the existing agent add cheaply (call sheets, email drafts, PDFs)?
5. ⬜ Copywriting: headline, 3 benefits, one example, one call to action.

## Answers

### Q1: the buyer (Drew, 2026-09-12)

- Worry: a "call list" is underwhelming. RealPage probably already has lead lists, so why would they want ours?
- Drew's idea: **one agent in the chat that does the whole sales research job, start to finish**:
  1. **Scout areas**: suggests places with untapped opportunity; Drew picks one.
  2. **Build the lead batch**: finds every building in that area and its software (today this only runs on Drew's local harness; move it into the chat agent).
  3. **Rank**: talk with the agent about which buildings to call first.
  4. **Deep dive**: for the top few, the agent researches that one building (Jina web lookups) and writes a brief on *why* they'd switch and what to say.
  5. **Area log**: remembers every area explored, so the coverage map grows over time.

### My take (to test with Drew)

- The new value isn't the list. It's **"an AI sales researcher that works a whole city for you and hands each rep a reason to call."** Lead lists are common; a per-building "why now" brief is not.
- Signals for picking an area: lots of new apartment construction, growing renter counts, many buildings on rival software, recent building sales.
- What a salesperson needs in the brief ("trigger events"): a new owner or new management company (new managers often switch software), a new building in lease-up, resident reviews complaining about the portal or payments, unit count, current software, and who to contact.
- Bonus for the interview: this shows exactly the job skills (agent workflows, tools, logs, human picks the next step).

### Q2: the demo flow (Drew, 2026-09-12)

- Drew: A, B and C aren't hard, since most of it is already skills. Refine every skill in the local harness first, then move them into the chat agent. Skip D (the coverage map).
- ✅ = skill exists today · 🆕 = new skill to build · ⚠️ = risk

```
 SALESPERSON (in the chat)                AGENT                                   OUTPUT
 ─────────────────────────                ─────                                   ──────
 "Where should we sell next?"
            │
            ▼
 ┌──────────────────────────────────────────────────────────────────────────────────────┐
 │ 1. SCOUT AREAS  🆕 scout-areas                                                       │
 │    IN:  a region ("Texas") or nothing                                                │
 │    DO:  compare cities: apartment count, new construction, recent building sales,   │
 │         renter growth                                                                │
 │    OUT: 3–5 area cards: "Frisco: 140 buildings, 12 sold last year, 9 going up"      │
 └──────────────────────────────────────────────────────────────────────────────────────┘
            │  person picks one  ◄── HUMAN CHOICE
            ▼
 ┌──────────────────────────────────────────────────────────────────────────────────────┐
 │ 2. BUILD THE AREA  (runs the existing pipeline, from chat)  🆕 run-area wrapper      │
 │    ✅ find-apartments  → every building with 20+ units (county records)             │
 │    ✅ find-website     → each building's own site                                   │
 │    ✅ detect-software  → RealPage / Yardi / Entrata / ...                           │
 │    ✅ build-table      → one master table                                           │
 │    ✅ find-sales       → who changed owners  ⚠️ built for Collin County only        │
 │    ✅ find-upcoming    → what's being built  ⚠️ built for Plano only                │
 │    ✅ contact-scrape   → phone / email                                              │
 │    OUT: progress in chat ("212 buildings found… 148 sites… 97 on Yardi")            │
 │         + the area is saved and logged (runs log)                                    │
 └──────────────────────────────────────────────────────────────────────────────────────┘
            │  takes minutes: agent reports progress, person waits
            ▼
 ┌──────────────────────────────────────────────────────────────────────────────────────┐
 │ 3. RANK  ✅ score-leads  + ✅ query-propertystack (chat Q&A)                         │
 │    IN:  "Who should I call first?"                                                   │
 │    OUT: top 10 with one-line reasons; person can push back: "skip the tiny ones"    │
 └──────────────────────────────────────────────────────────────────────────────────────┘
            │  person picks 1–3 buildings  ◄── HUMAN CHOICE
            ▼
 ┌──────────────────────────────────────────────────────────────────────────────────────┐
 │ 4. DEEP DIVE  🆕 building-brief  (Jina web search + reading pages)                  │
 │    DO:  new owner? new manager? lease-up? resident complaints about portal/payments?│
 │         who runs it, who to contact, current software and its known pain points    │
 │    OUT: a one-page "why call now" brief, every fact with a source link             │
 └──────────────────────────────────────────────────────────────────────────────────────┘
            │
            ▼
 ┌──────────────────────────────────────────────────────────────────────────────────────┐
 │ 5. READY TO CALL  🆕 call-kit                                                        │
 │    OUT: opening line, 3 talking points, likely objections + answers, email draft,   │
 │         downloadable call sheet (PDF)                                                │
 └──────────────────────────────────────────────────────────────────────────────────────┘
```

### Gaps this flow exposes

- 🆕 Four new pieces: scout-areas, run-area (start the pipeline from chat), building-brief, call-kit.
- ⚠️ The chat is read-only today ("must NOT change data"). Step 2 means the agent writes new areas. Probably fine if it only *adds* areas and never edits existing ones.
- ⚠️ Sale and new-construction finders only know Collin County and Plano. A new area needs its own county/city sources, which is the hardest part of "any area".
- ⚠️ Step 2 takes minutes and costs web-lookup credits per building; the chat needs to show progress and cap the size.

### Q2b: scope and how we build it (Drew, 2026-09-12)

- **The flow ends at the call.** Pick who to call → learn about them → call kit. No logging calls, no follow-ups, no CRM.
  - Saving happens automatically anyway: the chat app already keeps each signed-in user's chats (per Google account). The agent has no memory *between* chats, and nothing records areas explored. Fine for now.
- **Build order:**
  1. Build/refine every step as a skill in the **local harness** (Claude Code in this repo, `propertystack/skills/`), not in the chat agent yet.
  2. **Prove the whole flow end to end on one new area** inside a local session.
  3. Only then give the same skills to the chat agent.
- **Architecture rule: kindergarten simple.** One skill = one step = one folder (`SKILL.md` + `run.py`). Each reads only the files before it and writes one numbered file. Same pattern the 7 finder skills already use.
- **Chat agent today (for the gap list):** reads data + research, web search/read, call prep in text, keeps per-user chat history. It cannot: run skills/new areas, write data, make files (PDF), remember across chats.

### Q2c: step 1 = find a new area where RealPage is weak (Drew, 2026-09-12)

- Memory, plainly: each user's chats are **saved and listed** (like ChatGPT's sidebar) and the agent remembers everything **inside** a chat; a **new chat starts blank** (no memory across chats).
- Drew: the flow starts with **finding a new area opportunity**, and it must be an area where **RealPage doesn't already have a strong hold**.
- We can't see RealPage's sales territories (outside-in), so "RealPage is weak here" = measured from public data:
  - **RealPage share** of buildings in a quick sample per city (our detect-software, ~20–30 buildings per city).
  - **Growth**: new apartment permits (Census Building Permits Survey, free) and renter households (Census ACS, free).
  - **Size**: total apartment buildings/units.
- Proposed skill `scout-areas`: candidate cities → sample + growth + size → ranked area cards ("Denton: 180 buildings, RealPage 8% of sample, 1,900 new units permitted last year").

### Q2d: what tools the chat agent needs (proposal, 2026-09-12)

Principle: **the skills are the work; the agent only gets a few thin tools to start them and read their results.** Same skill scripts run in the local harness and in the chat, so what we prove here is what the chat runs.

| Tool | Status | What it does |
|---|---|---|
| `ps_run_skill(skill, area, options)` | 🆕 | Starts one **allow-listed** skill script (scout-areas, run-area, building-brief, call-kit) in the background. Only ever writes a **new** area folder or a new brief; never edits existing data. Returns a job id. |
| `ps_job_status(job_id)` | 🆕 | Progress + result file ("step 3/7 detect-software: 148/212"), so the chat can report while the person waits. |
| `ps_schema` / `ps_sql` | ✅ extend | Today: one area baked into the image. Needed: query **any** area folder on a storage drive. |
| `ps_web_search` / `ps_web_read` | ✅ | Jina, already live. Used by building-brief and by the agent directly. |
| `ps_make_file` (PDF/CSV) | 🆕 later | Downloadable call sheet (call-kit step 5). |

Also needed outside the agent: area data on a **volume** (not baked into the image), the dashboard reading areas from it (area picker), per-run caps (buildings, Jina calls) and only signed-in users can start runs.
Scout's own data (Census permits/renters, sampled software share) is fetched **inside the scout-areas skill**, not as separate agent tools.

### Q2e: memory across chats (Drew, 2026-09-12)

- Drew wants the agent to remember each person across chats (like Eve does).
- Today: refresh/reopen keeps the same chat (saved per user); only **New chat** starts blank.
- Why it's off: Hermes memory is **one bucket for the whole agent**. Fine for Eve (one user), a leak here (one rep's notes in another rep's answers). `memory_enabled: false` on purpose.
- Proposed fix: Open WebUI's built-in **per-user Memory** (Settings → Personalization → Memory): each signed-in user's memories are added to their own chats only. Mostly config + a test that user A's memory never shows for user B.
- **Drew (update): shared memory for everyone instead.** One team memory: what one rep learns, every rep's agent knows in every new chat.
  - How: turn Hermes' built-in memory back on (it is already one shared bucket).
  - Risks: anything said can reach everyone's answers; a web page could plant a fake "fact" that sticks.
  - Guardrails: save only work facts (buildings, areas, market), never unsourced web text, nothing personal; admin can view and clear it.
- **Drew's pick: C, no rules.** The agent saves anything it finds useful into the shared team memory. (Risks above accepted; admin can still view/clear.)

## Snapshot: the whole plan so far (2026-09-12)

### The product in one line
An AI sales researcher inside the dashboard that **finds a city where RealPage is weak, maps every apartment building there and its software, ranks who to call, researches the top buildings, and hands the rep a call kit.** Ends at the call.

### Decided
- Buyer story: RealPage sales (lists are common; the per-building "why call now" brief is not).
- Flow: scout area → build area → rank → deep dive → call kit. No call logging / follow-ups.
- Build skills in the local harness first, prove on one new area in a session, then give the same skills to the chat agent.
- Kindergarten architecture: one skill = one step = one folder, reads the files before it, writes one file.
- Chat agent gets thin tools: run-skill (allow-listed, add-only), job-status, sql over any area, web search/read, make-file later.
- Shared team memory, no rules (C).

### Holes still to fill
1. Scout rule: what makes an area an "opportunity" (low RealPage share + growth was the pick, not confirmed).
2. How wide areas can be: sale + new-build finders only know Collin County / Plano.
3. Per-area cost and size caps (Jina calls per building; not measured yet).
4. Who to contact: management company only, or find the decision-maker person too?
5. Call kit format: text in chat vs downloadable PDF.
6. Storage + area picker so new areas show up in the dashboard and the chat.
7. "Production-ready to sell": onboarding copy, proof, price (Q2–Q5 above).
8. Other plan items parked: Builder mode (edit site/bot from chat), "At Risk" section (RealPage buildings that just sold).

### Q3: the flow, re-cut into two halves (Drew, 2026-09-12)

Drew's decisions:
- **Heavy research runs on Drew's computer only** (the local harness = this Claude Code setup + crawl4ai + Reddit tools + web search): a "research army" for scouting and building areas. The chat agent does **not** scout or build areas.
- **Scout outside RealPage's home turf** (not the Dallas area; RealPage HQ is Richardson). Answer to "how wide": pick from other metros.
- The scout's first job is **reasoning**: define what a good untapped area is and argue why RealPage would win there.
- The site's product is the **Early Leads table per area**. Clicking a lead opens its building page, which gets a **"Deep dive in chat"** button that drops that building into the AI chat; the deep dive and call kit happen in the conversation.
- **Drop the Master Table page.** Keep Software Share. Under the Hood later.
- Still planning; the build runs with `/gowork` once the plan is done.

The flow, end to end:
```
ON DREW'S COMPUTER (local harness, automatic after each pick)
 1. Scout      "find untapped areas"  → research + reasoning → 3–5 area cards        ◄ Drew picks one
 2. Build      run the finder chain on that area → buildings, websites, software,
               sales, upcoming, contacts
 3. Rank       score-leads → Early Leads table for the new area
 4. Publish    area goes onto the site (area picker)
ON THE SITE (salesperson)
 5. Early Leads → click a building → building page
 6. "Deep dive in chat" button → chat opens with that building already filled in
 7. Chat agent: why-call-now brief (web research) → call kit. Flow ends at the call.
```

What this removes: the chat no longer needs run-skill / job-status tools or write access; it only reads areas + web. Shared team memory still on.

### Q4: what makes an area "untapped" (Drew: D, all three combined, 2026-09-12)

The scout scores each candidate city on three parts and writes a short "why this city" argument for each:
1. **Growth + weakness**: many apartments, many new units being built (Census permits), low RealPage share in a sampled set of buildings.
2. **Churn signals**: recent building sales and management-company changes (owners re-pick software then).
3. **Competitor pain**: Yardi/Entrata-heavy, with residents and managers complaining online about portals/payments (Reddit tools, web search, crawl4ai).
Output: 3–5 area cards, each with the three part-scores, the evidence links and a plain-English reason. Drew picks.

### Q5: where to scout + the map (Drew, 2026-09-12)

- **Scout region: B, the Sun Belt** (Texas, Florida, Arizona, the Carolinas), outside RealPage's Dallas home turf. Drew wants it to be really good, not the easy path. Accepts that each new state/county needs its own record sources.
- **Replace the Master Table page with a simple US map.** Very easy to read, nothing fancy: a map of the US with dots.
  - Dots for **where RealPage's clients are**.
  - When the local scout runs, it **tags the areas it is researching** with a marker, so you can see where it's hunting for untapped opportunity.
- ⚠️ Outside-in limit: we don't have RealPage's client list. Every RealPage dot must come from public evidence (see Q6).

### Q6: what the map dots mean (Drew: D, 2026-09-12)

- Dots = **RealPage's public reach**: where they visibly are (offices, case studies, press-release customers, plus RealPage buildings we've proven). A rough "where they are / where they aren't" picture, not a building-precise client list.
- Purpose: see where RealPage hasn't reached yet → where to go. Scout markers show which areas the local scout is researching.

### Q7: chat readability (Drew, 2026-09-12)

- The site agent's answers are hard to read: too much text, packed together, too small.
- Rule: long answers are fine, but **every answer ends with a short, clear summary of what matters** (a few bullets). More spacing, bigger text, less clutter.

### Q8: split into separate plans, build fast (Drew, 2026-09-12)

- Drew stops adding scope; only fills holes. Each plan must be precise, then built **fast** with `/gowork`, on **localhost first**.
- Proposed split (independent plans can build at the same time):
  1. **Chat readability**: agent format rules (summary at the end) + bigger, airier chat text. Tiny.
  2. **Site: map + deep-dive button**: US map page replaces Master Table (RealPage reach dots + scout markers); "Deep dive in chat" button on building pages that fills the chat.
  3. **Shared team memory**: turn on, no rules.
  4. **Local scout skill**: scout-areas (growth+weakness, churn, competitor pain) for Sun Belt metros → area cards + map markers.
  5. **New area end to end**: build one picked Sun Belt area (new county sources for sales/new builds) → Early Leads on the site.
  - Parked: Builder mode, "At Risk" section.
- Order: 1, 2, 3 can run at once; 4 then 5 (5 needs the scout's pick).

### Holes, one at a time (2026-09-12)
- **H1 map dot click (Drew: A):** a small card: city, how many RealPage signs we found, and the proof links.
- **H2 deep-dive button (Drew: A):** on each building page **and** a small button on every Early Leads row. Click fills the chat with that building; the person presses send (no auto-run).
- **H3 scout size (Drew: C):** ~25 Sun Belt metros per run, incl. mid-size (Tucson, Boise, Greenville…). Use the fastest local tools: **crawl4ai** (already running locally, free) for reading pages, Jina for search, Reddit tools for competitor pain.
- **H4 scout cost cap (Drew: A):** max ~1,500 Jina searches per run (~60/metro); on hitting it the scout stops and reports what it finished.
- **H5 missing county records (Drew: A):** always build buildings/websites/software; if a county has no online sales or construction records, publish anyway with a clear "not available here" note and rank on what we have. Default size cap per area: 400 buildings (raise on request).
- **H6 who writes the scout reasons (Drew: A):** the local Claude session reads the scout numbers + evidence and writes the cards; run.py produces numbers/links only.
- **H7 deep dive lives in the site chat, but is designed here first (Drew):** the site agent does the deep dive in the conversation. Before that, we pin down **exactly what the deep dive outputs** by building and running it as a skill in this local harness (`propertystack/skills/deep-dive/`, fixed output format), testing it on real leads, then giving the same recipe to the site agent. New plan: PLAN-deep-dive.md.
- **H8 find the decision-maker (Drew: A):** the deep dive searches public pages for the regional/operations person at the management company, with a proof link. Never guessed emails or phone numbers.
- **H9 deep-dive layout (Drew: A):** one fixed layout every time: Bottom line · Who they are · Why now (sale, new manager, complaints) · Who to ask for · 30-second opener · 3 questions · 2 objections + answers · Sources.
- **H10 summary placement (Drew: A):** deep dives start with the Bottom line; normal answers end with it.
- **H11 taking the call sheet away (Drew: A):** text in the chat + the chat Copy button. No PDF.
