Status: GENERATED v1 (2026-09-10) — see images/frame-03-software-share.jpg — 30 credits (Nano Banana 2, 16:9)

# Frame 3: Software Share

Top bar: see `00-global-top-bar.md`.

What's on it, top to bottom:
1. Two charts side by side: left = share by number of properties (donut or
   bar chart), right = share by number of units (donut or bar chart).
2. Table below the charts, columns: Software · Properties · Units · % ·
   Change since last run.
3. "View as" (from the top bar) highlights whichever vendor is selected in
   both charts and the table; clicking a vendor row/segment jumps to Master
   Table filtered to that vendor (describe this as a hover/click affordance,
   not literal in a static image).

Real sample data (identified so far, from the brief): Yardi 14 · RealPage 8
· Entrata 3 · Yotta 1 (26 identified total out of 195).

## Draft Blotato prompt (two-stage plan)

**Stage 1 — grayscale structure pass:**
> Monochrome UX wireframe of a web app dashboard screen, top navigation bar
> with wordmark, four tabs, and right-side dropdown plus toggle, below it
> two square chart placeholders side by side — left one a simple donut
> chart outline, right one a simple bar chart outline, each with a small
> title above it, below the charts a simple table with five columns and
> four to five rows, hand-drawn wireframe sketch aesthetic, white
> background, no color, Balsamiq style, desktop web layout, wide aspect ratio.

**Stage 2 — styled pass (image-edit model, using stage 1 as input):**
> Apply a clean enterprise SaaS visual style to this dashboard wireframe:
> navy blue top bar, white background, the donut and bar charts colored
> with a small distinct muted color per software vendor (consistent across
> both charts and the table pills), a light gray card background behind
> each chart, teal accent for chart titles, table rows with small colored
> software pills matching the chart colors, a small up/down arrow and
> percentage in the "change since last run" column colored green or red,
> modern bold sans-serif headers, lighter sans-serif table text, corporate
> and data-confident tone, high-fidelity UI mockup, not photorealistic.

## Model choice
- Stage 1: cheap model (Flux Schnell, 1 credit) — generate 2 layout options.
- Stage 2: edit-capable model (Nano Banana Edit or similar) on the chosen one.
